# このリポジトリで守ること

## コメントに issue 番号を書かない

`（5-3）` `（8-1）` `（YOSUKE-41）` のような issue・フェーズの番号を、コード内の
コメントや docstring に残さないこと。PR本文・コミットメッセージには書いてよい
（そちらは Linear と紐づける役目がある）。

**なぜ**

- 番号だけでは何のことか分からない。読む側は Linear を開かないと意味を取れず、
  コードの中で完結しない。
- issue は閉じたり付け替えられたりするが、コードは残る。番号は真っ先に
  古くなる情報で、「今どうなっているか」を説明しない。
- `5-1 の時点では` のような書き方は、それが「昔の話」なのか「今の仕様」なのかを
  番号を知っている人にしか伝えない。

**代わりに書くこと**

番号で指していた内容を、そのまま日本語で書く。時期を言いたいなら、番号ではなく
コード上の出来事（取り込み時 / 索引作成時 / 回答生成時…）で表す。

```python
# NG: 番号がないと何も言っていない
# 画像チャンクは 5-1 の時点では embedding を持たない（検索対象化は 5-2）

# OK: いつ・なぜ、がコードの中で完結する
# 画像チャンクは登録した時点では embedding も content_nouns も持たないため
# 検索にはヒットしない（検索対象化は索引作成で別途行う）
```

```python
# NG
"""コンテキストに載せる原本画像1枚（5-3）。"""

# OK
"""コンテキストに載せる原本画像1枚。

言語化テキスト（キャプション）ではなく原本を渡すのは、説明文に書かれなかった
ことを後から問えないため。
"""
```
