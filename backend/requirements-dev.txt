# テスト用の追加依存（本番イメージには入れない）。
#   docker compose exec backend pip install -r requirements-dev.txt
# もしくは Taskfile の `task test` から使う。
-r requirements.txt
pytest
# lint（flake8 + isort 相当）。設定は backend/ruff.toml。`task lint` / `task fmt` から使う
ruff
