# frontend

Next.js + Vercel AI SDK のチャットUI。

## TODO
- `npx create-next-app@latest` でここに初期化（App Router）
- Vercel AI SDK (`ai` パッケージ) の `useChat` でストリーミングチャット
- バックエンド（FastAPI /chat）へプロキシ
- 回答内の根拠文書リンク（S3署名URL）を表示

旧版の Chatbot UI フォークは引き継がない（設計書 第9章）。
